#pragma once

// Mechanical and safety limits (placeholders; tune later)
#define LIMIT_MAX_SPEED_MPS     (8.0f)
#define LIMIT_MAX_ACCEL_MPS2    (5.0f)

// Communication timeouts (ms)
#define TIMEOUT_HOST_HEARTBEAT_MS   (500)
#define TIMEOUT_RTK_MS              (200)
#define TIMEOUT_RANGE_MS            (200)
